# Лог задачи: апгрейд SAST-агента до AI-агента

- 2026-05-20: пользователь запросил реализовать все пункты плана (производительность, охват, AI-агентный слой, CI-выводы).
- При старте отсутствовал `ai_project_knowledge/README.md`; создан в рамках задачи, чтобы соответствовать `AGENTS.md`.
- Реализован параллельный скан (`workers`) и метрики сканирования.
- Реализован инкрементальный режим с кэшем хешей `.sastcache.json`.
- Добавлен режим `changed-only` по `git diff`, с fallback для репозиториев без коммита.
- Расширены правила: taint-lite для command injection, SSRF, path traversal; review-rule для temp files; rule для unsafe deserialization.
- Добавлен слой AI-агента: `TriageProvider`, `NoopProvider`, `OllamaProvider`, оркестратор `agent.Run`.
- В отчет добавлен `agent_verdict` (label/confidence/rationale/provider).
- Добавлен SARIF-вывод для CI/code scanning.
- Добавлен `mode fast|deep` для эксплуатационных сценариев (PR vs deep run).
- Реализован feedback loop: `-feedback-out` создает шаблон решений ревьюера, `-feedback-in` применяет решения как override triage.
- Добавлен контейнерный контур: `Dockerfile`, `.dockerignore`, `docker-compose.yml`, контейнерный запуск `sast-agent` + `ollama`.
- Обновлен `README.md` в container-first формате; все основные команды запуска переведены на `docker compose`.
- Проверка `docker compose config --quiet` успешна.
- Реализован полный agentic CI-контур: `-format ci`, `-ci-output-dir`, `-ai-budget`, `-fallback-provider`, policy-флаги `-fail-on-severity` и `-fail-on-verdict`.
- Добавлен policy engine и сборка CI-артефактов (`agent-report.json`, `agent-report.sarif`, `summary.md`).
- Проверочный запуск `-format ci` создает все артефакты в `.artifacts/sast` и корректно возвращает `exit 1` при срабатывании policy.
- Интерфейс упрощен: флаги сокращены до `-path` и `-format`; остальная конфигурация переведена в переменные окружения `SAST_*`.
- Добавлен `.env.example` и обновлен `docker-compose.yml` под env-driven запуск.
- Проведен реальный e2e-тест на GitHub-проекте `securego/gosec` (клонирование в `.tmp/gosec`).
- Запуск `deep` в контейнере с реальным Ollama (`opencoder:1.5b` через `host.docker.internal`) создал CI-артефакты в `.artifacts/gosec-ci`: `agent-report.json`, `agent-report.sarif`, `summary.md`; findings=11, ai_triaged=11.
- Выявлен и исправлен инфраструктурный дефект: для `fast/changed-only` в контейнере требовался `git`; добавлен `apk add --no-cache git` в `Dockerfile`.
- Проверен `fast` режим после фикса: артефакты создаются в `.artifacts/gosec-ci-fast`.
- Усилен AI triage: добавлен `ollama-agentic` (analyst -> skeptic -> judge) и передача code snippet вокруг finding.
- Добавлен устойчивый JSON extract для ответов моделей с лишним текстом.
- Выполнен e2e прогон `ollama-agentic` на `securego/gosec` с `SAST_AI_BUDGET=1`: findings=11, ai_triaged=1, fallback=10, зафиксирован реальный вклад agentic-провайдера в итог.
- Добавлена документация задания и исследований библиотек в `ai_project_knowledge/README.md` и `ai_project_knowledge/services/go_sast_libraries.md`.
- Ядро сканера улучшено: добавлен путь сканирования через `go/packages` + `ast/inspector` с fallback на file-level parsing.
- Добавлен серверный режим `cmd/sast-agent-server` (`/healthz`, `POST /scan`) для постоянной работы на сервере и выдачи verdict на каждый вызов.
- Docker-образ обновлен до `golang:1.24-alpine` в builder для совместимости с `x/tools`.
- В `docker-compose.yml` добавлен сервис `sast-agent-api` (порт `8080`) для удаленного CI-вызова.
- Добавлен шаблон GitHub Actions `.github/workflows/sast-agent-server.yml` для интеграции с удаленным SAST API сервером.
- `go test ./...` проходит после обновления ядра.
- Добавлен `docker-compose.server.yml` для прод-развертывания: readonly fixed mount (`/scan-target`), `read_only: true`, `tmpfs`, и CPU/RAM limits.
- Полностью переписан основной `README.md` в прозрачную документацию: архитектура, API контракт, server mode, prod deployment и CI integration.
- Проверена валидность обоих compose-файлов: `docker compose config --quiet` и `docker compose -f docker-compose.server.yml config --quiet`.
- Устранена ошибка `no space left on device`: очищен Docker build cache (`docker builder prune -af`), освобождено ~14.21GB.
- Устранено предупреждение `pull access denied for mbks-sast-agent`: добавлен `pull_policy: never` для локально-собираемых сервисов в `docker-compose.yml` и `docker-compose.server.yml`.
- Устранен macOS mount blocker (`path is not shared from the host`): default `SAST_TARGET_PATH` в `docker-compose.server.yml` изменен на `./scan-target`; в `README.md` добавлена явная инструкция по Docker File Sharing.
- По запросу пользователя удален fallback-контур: в CLI/API оставлены только провайдеры `ollama` и `ollama-agentic`; при ошибке AI запрос завершается ошибкой.
- Добавлен обязательный прогрев модели в контейнере: сервис `ollama-init` с `depends_on: service_completed_successfully`; `sast-agent-api` стартует только после успешного `ollama pull`.
- Для `ollama-init` исправлен host (`OLLAMA_HOST=http://ollama:11434`), чтобы pull выполнялся против контейнерного Ollama, а не localhost внутри init-контейнера.
- Обновлены тесты: инкрементальный пропуск неизмененных файлов и taint-lite detection.
- Прогон: `go test ./...` (через локальный `GOCACHE`) успешен.

Кандидат на перенос в постоянную документацию: в проекте поддерживается режим реального локального AI-триажа через Ollama (`/api/generate`), при недоступности модели fallback должен оставаться `noop`.
- 2026-05-23: финальная версия проекта — полный рефакторинг и CI-интеграция.
  - Исправлен Dockerfile: был ошибочно равен Dockerfile.codeql; заменён на лёгкий Alpine образ.
  - Dockerfile.codeql: golang:1.25 → golang:1.24 (1.25 не существует).
  - Удалены пустые файлы-заглушки: rules_c.go, rules_python.go, rules_csharp.go, scripts/container_scan.sh.
  - .env.example очищен от CLI-переменных (SAST_FORMAT, SAST_MODE, SAST_INCREMENTAL и др.); добавлены облачные провайдеры.
  - ci_bundle.go: WriteCIBundle теперь принимает shouldFail bool; buildSummary генерирует полноценный markdown с collapsible findings — используется как MR-комментарий.
  - cmd/sast-agent/main.go: policy вычисляется перед WriteCIBundle, shouldFail передаётся явно.
  - .github/workflows/sast-agent-server.yml полностью переписан: multipart-загрузка архива (не JSON), actions/github-script постит/обновляет PR-комментарий с AI-вердиктами, маркер <!-- sast-bot --> предотвращает дублирование.
  - Добавлены: examples/gitlab-ci.yml (GitLab CI с MR-комментарием через API), scripts/format-mr-comment.sh (shell+jq форматтер).
  - docs/architecture.md актуализирован: все провайдеры, server mode, agentic pipeline.
  - README.md переписан: правильный API контракт (multipart), таблица параметров, секция CI/CD с GitHub Actions и GitLab.
  - go build ./... и go test ./... — чистые.
- 2026-05-23 (продолжение): CI-примеры перенесены в examples/, добавлен deploy workflow и тест MR-комментариев.
  - examples/github-actions.yml — пример CI с постингом PR-комментария (скопировать в .github/workflows/sast.yml целевого репозитория).
  - examples/gitlab-ci.yml — пример GitLab CI с MR-комментарием.
  - .github/workflows/deploy.yml — деплой на веб-сервер при пуше в main: SSH + SCP + docker compose -f docker-compose.server.yml; secrets: DEPLOY_SSH_KEY, DEPLOY_HOST, DEPLOY_USER; vars: DEPLOY_PATH, SAST_OLLAMA_MODEL, SAST_AI_PROVIDER.
  - .github/workflows/test-mr-comment.yml — тест интеграции: при открытии PR постит реальный комментарий через GitHub API (mock fixture), затем верифицирует его наличие и контент через actions/github-script.
  - scripts/deploy-production.sh — серверный скрипт: docker compose up --build + healthcheck с retry.
  - tests/fixtures/scan-response.json — мок-результат на основе examples/vuln-api/main.go (5 находок: command injection, hardcoded secret, SQL injection, SSRF, weak hash).
  - tests/test-mr-comment.sh — локальный тест формата комментария; sh tests/test-mr-comment.sh → 10/10 passed.
  - sast-agent-server.yml удалён.
  - go build ./... и go test ./... — чистые.
- 2026-05-23: улучшена устойчивость triage для локальных LLM (Ollama) без смены сканер-логики.
  - Усилен prompt в `internal/agent/provider/prompt.go`: для HIGH/CRITICAL добавлено правило default=true_positive, `needs_review` только при явно описанном блокирующем контексте; запрет false_positive без конкретной защиты в сниппете; явное требование русского языка для rationale/remediation.
  - Добавлена пост-калибровка verdict в `internal/agent/calibration.go` и интеграция в `internal/agent/agent.go`: нормализация label/confidence, переклассификация спорных `false_positive/needs_review` для high-risk кейсов при отсутствии признаков mitigation, с логом причины переклассификации.
  - Добавлены диагностические логи парсинга ответа модели в `internal/agent/provider/single.go` и `internal/agent/provider/agentic.go` (truncate raw/analysis/critique).
  - Добавлены тесты `internal/agent/calibration_test.go` на ключевые сценарии калибровки.
  - Прогон в sandbox: `GOCACHE=$(pwd)/.tmp/gocache go test ./...` — успешно.

Кандидат на перенос в постоянную документацию: для локальных моделей введена детерминированная post-triage калибровка high-risk verdict, чтобы снизить систематический сдвиг в `needs_review`/`false_positive`.
- 2026-05-23: исправлены ложные срабатывания GO-HARDCODED-SECRET и автокалибровка при сбое AI.
  - `secretLike` — добавлен фильтр несекретных суффиксов (Controller, Handler, Manager, Provider и др.).
  - `secretValueLike` — заменила `stringLongEnough`; срабатывает при len >= 16 или PEM-блоке.
  - `calibrateVerdict` теперь вызывается только при `r.err == nil`; при сбое AI verdict остаётся `needs_review`.
  - `go build ./...` и `go test ./...` — чистые.
- 2026-05-23: исправлен механизм запуска gosec и govulncheck.
  - `CombinedOutput()` заменён на раздельные `cmd.Stdout`/`cmd.Stderr` буферы в `ScanWithGosec` и `ScanWithGovulncheck`.
  - Ранее JSON-вывод смешивался с текстовым stderr (баннер gosec), что ломало `json.Unmarshal`.
  - Теперь ошибка возвращается только если stdout пуст; stderr игнорируется.
- 2026-05-23: обновлена документация уровня релиза.
  - README.md: описание 3-уровневого сканирования, калибровка, исправлена дублированная строка проверки.
  - docs/architecture.md: полностью переписан под текущую архитектуру.
  - docs/DOCUMENTATION.md: обновлены разделы 1, 2, 3, 4, 9, 12, 13; удалены ссылки на удалённые файлы (rules_c.go, rules_python.go, rules_csharp.go, scanFilesParallel); добавлен раздел gosec/govulncheck; Dockerfile — один образ.
  - ai_project_knowledge/ обновлён.
  - Выполнен init-коммит и push в main.
- 2026-05-24: исправлена автокалибровка false_positive для HIGH/CRITICAL.
  - Было: `false_positive` без митигации при HIGH/CRITICAL → принудительно `true_positive` (игнорировало вердикт модели).
  - Стало: максимальный апгрейд — `needs_review`; `true_positive` ставит только сама модель.
  - Обновлён тест `TestCalibrateVerdict_FalsePositiveWithoutMitigationToNeedsReview`.
  - `go test ./internal/agent/...` — чистый.
