# 2026-05-23 git remote

- Прочитан `ai_project_knowledge/README.md`.
- Тематические файлы не открывались: задача только про настройку git remote.
- До изменения `git remote -v` не показывал настроенных remote.
- Добавлен remote `origin` -> `https://github.com/zenrot/vulnScaner`.
- Проверено: `git remote -v` показывает `origin` для fetch/push; `git ls-remote --heads origin` выполнился без ошибки, но не вернул heads.
